import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    primary: '#282c34', // dark dark blue undecided
    secondary: '#E9E6FF', // undecided
  },
});

export default theme;